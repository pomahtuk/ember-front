{{experiments-toolbar data=controller}}

{{#each experiment in controller}}
  {{single-experiment experiment=experiment graphSettings=controller.graphSettings}}
{{else}}
  no experiments… :-(
{{/each}}


{{outlet}}
