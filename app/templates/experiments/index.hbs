Experiments:

<ul class="experiments-listing">
  {{#each experiment in controller}}
      <li>
        {{#link-to 'experiment.index' experiment}}
          {{experiment.name}}
        {{/link-to}}
      </li>
    {{else}}
      <li>no experiments… :-(</li>
  {{/each}}
</ul>

{{#link-to "experiments.create" class="button tiny"}}New{{/link-to}}

{{outlet}}
